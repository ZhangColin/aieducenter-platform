-- ========================================================================
-- Account Context: 重命名用户表（acc_users → act_users）
-- ========================================================================

-- PostgreSQL / MySQL
ALTER TABLE acc_users RENAME TO act_users;
