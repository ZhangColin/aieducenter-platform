spring:
  datasource:
    url: jdbc:postgresql://localhost:5434/aieducenter_test
    username: aiedu_test
    password: test123
    driver-class-name: org.postgresql.Driver

  data:
    redis:
      host: localhost
      port: 6380

  flyway:
    enabled: false

  jpa:
    hibernate:
      ddl-auto: update
